name: Hourly Snapshot
on:
  schedule:
    - cron: '0 * * * *'
jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - run: curl -X POST -H "x-cron-secret:purrlend_points_secret_2026" https://purrlend-points-api-production.up.railway.app/api/points/snapshot
